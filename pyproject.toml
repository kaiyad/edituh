[project]
name = "edituh"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.59.1",
    "streamlit-hotkeys>=0.6.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

